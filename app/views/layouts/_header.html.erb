<nav class="">
    <ul>
        <li> <%= link_to "Store Home", store_path %> </li>
        <li> <%= link_to "sign in", new_user_session_path if !user_signed_in? %></li>
        <li> <%= link_to "Sign up", new_user_registration_path if !user_signed_in?%></li>
        <li> <%= "Signed in as #{current_user.email}" if user_signed_in? %></li>
        <li> <%= link_to "Sign out", destroy_user_session_path if user_signed_in?%></li>
        <% if !!@cart %>
          <li> <%= link_to "Cart", cart_path(current_user.current_cart) %></li>
        <% end %>
    </ul>
</nav>
